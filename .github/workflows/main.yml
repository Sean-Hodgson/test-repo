- name: Assign Issue
  uses: actions-ecosystem/action-assign@v2
  with:
    github-token: ${{ secrets.GITHUB_TOKEN }}
    assignees: sean-hodgson # Replace X with the GitHub username of the team member
